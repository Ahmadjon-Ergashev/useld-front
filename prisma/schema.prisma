generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model SiteContent {
  id        Int      @id @default(1)
  content   String
  updatedAt DateTime @updatedAt
}
